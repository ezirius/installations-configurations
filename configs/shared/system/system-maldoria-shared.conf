# Shared system overrides for host 'maldoria'.
#
# This file is intentionally minimal. It exists to establish the layered host
# override contract under configs/shared/system/.

DOCK_AUTO_HIDE=true
DOCK_REORDER_SPACES_BY_RECENT_USE=false
AC_POWER_SYSTEM_SLEEP_MINUTES=0