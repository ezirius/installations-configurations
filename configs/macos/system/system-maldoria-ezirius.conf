# macOS system overrides for host 'maldoria' and user 'ezirius'.
#
# This file applies to host 'maldoria' for user 'ezirius' under configs/macos/system/.
# It is the host-specific, specific-user layer in the system config resolution order.

SSH_REMOTE_LOGIN_ENABLED=true
SSHD_ALLOW_USERS="ezirius"
SSHD_LOGIN_KEY_FILES="maldoria-iparia-ezirius-login.pub maldoria-ipirus-ezirius-login.pub"
