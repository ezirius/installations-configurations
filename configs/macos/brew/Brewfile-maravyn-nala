# Host-specific macOS Brewfile for host 'maravyn' and user 'nala'.
#
# Folder contract:
# - configs/<os>/<application>/
# - this file belongs to the 'brew' application under the 'macos' OS scope
#
# Filename contract:
# - Brewfile-<host>-<username>
# - 'shared' is valid in both the host and username slots
# - Brewfiles load from least specific to most specific:
#   shared.shared, shared.x, x.shared, x.x
# - shared OS scope loads before the concrete OS scope
#
# Content contract:
# - use only 'brew "..."' and 'cask "..."' entries
# - unsupported directives are rejected by scripts/shared/brew/brew-install

# CLI tools
# brew "bash"
# brew "mosh"
# brew "nushell"

# brew "opencode"
# brew "podman"
# brew "podman-compose"

# Apps
# cask "ghostty"
# cask "tailscale-app"

cask "brave-browser"
# cask "element"
# cask "home-assistant"
# cask "multipass"
# cask "podman-desktop"
# cask "rustdesk"
# cask "utm"
# cask "vscodium"
# cask "whatsapp"
