# Shared macOS Brewfile for user 'ezirius'.
#
# Folder contract:
# - configs/<os>/<application>/
# - this file belongs to the 'brew' application under the 'macos' OS scope
#
# Filename contract:
# - Brewfile-<host>-<username>
# - 'shared' in the host slot means all hosts for that OS scope
# - this file is loaded before any matching host-specific Brewfile in the same
#   application folder
#
# Content contract:
# - use only 'brew "..."' and 'cask "..."' entries
# - unsupported directives are rejected by scripts/shared/brew/brew-install

# CLI tools
# brew "bash"
# brew "mosh"
# brew "nushell"

brew "opencode"
brew "podman"
brew "podman-compose"

# Apps
# cask "ghostty"
# cask "tailscale-app"

cask "brave-browser"
# cask "element"
cask "home-assistant"
cask "multipass"
cask "podman-desktop"
cask "rustdesk"
cask "utm"
cask "vscodium"
